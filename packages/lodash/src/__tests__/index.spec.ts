import {omit, pick} from '../'

describe('omit', () => {
  it('should exist', () => {
    expect(typeof omit).toBe('function')
  })
})

describe('pick', () => {
  it('should exist', () => {
    expect(typeof pick).toBe('function')
  })
})
